class TodoItemModel {
  String title;
  String description;
  bool completed;
  bool selected;
  bool starred;

  TodoItemModel({this.title, this.completed, this.selected, this.description, this.starred});
}